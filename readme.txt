本版本改动：
支持重定向操作。
重定向对饮的状态代码常用为302，并且要包含一个响应头：Location，值为要求浏览器再次发起连接时请求的路径。
即：希望重定向连接。
重定向的响应中通常包含响应正文。
对此，我们队HTTPResponse进行重构，添加重定向功能。

1.在HTTPResponse中添加一个重定向的方法：
     void sendRedirect(String url)
          在该方法中我们重新定向相关设置操作
2.在注册和登录Servlet中没在执行完业务后改为使用重定向的方式跳转到提示页面。

